{
  "name": "gateway",
  "version": "1.0.0",
  "description": "Dieses Projekt stellt ein Gateway für die Verwaltung von Business-Kunden bereit. Es basiert auf Node.js, Express und TypeScript.",
  "main": "server.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:integration": "jest --testPathPatterns=integration",
    "test:unit": "jest --testPathPatterns=test.ts$ --testPathIgnorePatterns=integration",
    "test:verbose": "jest --verbose",
    "test:silent": "jest --silent",
    "build": "tsc",
    "dev": "ts-node-dev src/server.ts",
    "start": "node dist/server.js",
    "lint": "echo \"Linting not configured yet\"",
    "clean": "rimraf dist coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.1.0",
    "@types/supertest": "^6.0.3",
    "jest": "^30.2.0",
    "supertest": "^7.1.4",
    "ts-jest": "^29.4.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0"
  }
}
